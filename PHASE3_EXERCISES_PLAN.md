# FitSync 第三阶段：动作库与自定义动作规划

## Summary
第三阶段目标是把小程序从“可演示训练记录”推进到“可长期使用的训练工具”。主线只做动作内容能力：扩充动作库、重排动作选择页、支持自定义动作及编辑删除。不做训练计划、搜索、云同步、总结卡或主题系统。

## Key Changes
- 动作分类改为 6 类：`胸 / 背 / 肩 / 腿 / 手臂 / 核心`，把原来的“腹”升级为“核心”。
- 动作角色改为 4 类：`热身 / 主项 / 辅项 / 孤立`。这里的“热身”是动作类型，不自动影响组数；是否算正式组仍由记录 Sheet 里的“热身组”开关决定。
- 动作选择页改版：
  - 顶部横向分类切换：胸、背、肩、腿、手臂、核心。
  - 分类下方显示“常用”，文案就叫常用。
  - “常用”实际逻辑只取当前分类内最近练过的动作，最多 2 个。
  - 下方展示当前分类全部动作，按 `热身 → 主项 → 辅项 → 孤立` 分组或排序。
  - 右上角/弱化按钮提供 `+ 自定义`。
- 自定义动作 v1 支持新增、编辑、删除。
  - 字段：动作名、分类、角色、默认组数、默认重量、默认次数。
  - 从某分类进入新增时，默认分类就是当前分类。
  - 自定义动作在列表中和预置动作混排，并显示轻量“自定义”标识。
- 保持现有原生小程序架构，不引入 Taro、TypeScript、UI 库或云开发。

## Interfaces
- 扩展动作数据结构：
```js
{
  id,
  name,
  category,
  categoryName,
  role, // warmup | main | accessory | isolation
  roleName, // 热身 | 主项 | 辅项 | 孤立
  targetSets,
  defaultWeight,
  defaultReps,
  source // preset | custom
}
```
- 新增本地 storage：
  - `custom_exercises`：保存用户自定义动作数组。
- 记录数据建议补充：
  - 新训练记录写入 `exerciseId` 和 `exerciseSource`，用于计算当前分类“常用”。
  - 老记录没有这些字段时保持可读，不做强制迁移。
- 动作库工具函数调整：
  - 统一返回“预置动作 + 自定义动作”。
  - 分类筛选后再计算当前分类常用。
  - 删除自定义动作只影响动作库，不删除历史训练记录。

## Test Plan
- 分类显示为胸、背、肩、腿、手臂、核心。
- 每类预置动作扩展到 6-8 个，总量约 40 个。
- 点击分类后，动作列表只显示当前分类动作。
- 有当前分类历史记录时，“常用”显示最近练过的最多 2 个动作。
- 没有当前分类历史记录时，“常用”区域隐藏或弱化为空态，不显示其他分类动作。
- 新增自定义动作后，立即出现在对应分类列表。
- 编辑自定义动作后，列表和开始设置层使用最新默认值。
- 删除自定义动作后，动作列表不再展示，但历史记录仍正常显示。
- 选择“热身”角色动作开始训练时，训练记录逻辑不自动变成热身组。
- 现有训练会话测试继续通过，新增动作库/自定义动作测试覆盖 storage 合并与排序逻辑。

## Assumptions
- 第三阶段分支使用 `phase3-exercises`。
- 动作页文案使用“常用”，但实现语义是“当前分类内常用”。
- 自定义动作第一版不加图片、备注、动作教程、搜索和收藏。
- 常用排序第一版按最近完成训练时间，不做复杂频率权重。
- 本阶段继续本地存储，暂不做账号和云同步。

## 实际完成情况

- 已扩展为 6 分类动作库：胸、背、肩、腿、手臂、核心。
- 已补齐预置动作库，每类 6-8 个常见动作，并加入 `热身 / 主项 / 辅项 / 孤立` 角色。
- 动作页已改为横向分类切换，当前分类内展示“常用”和全部动作。
- 自定义动作已支持新增、编辑、删除，并与预置动作混排。
- 自定义动作和开始训练设置层均支持目标组数直接输入。
- 训练记录 Sheet 支持重量直接输入两位小数、次数直接输入正整数。
- 记录确认文案已改为 `记录本组`，延迟确认时长为 1.5 秒。
- 训练页大 `+` 按钮已增强尺寸、按下反馈和轻微呼吸光效。
- 撤销 Toast 已恢复 3 秒倒计时和独立撤销按钮，视觉为细边红。
- 防漏记提醒已恢复 `补记一组 / 忽略` 操作。
- 休息结束保留轻震动和视觉提示。

## 收尾验收清单

- [ ] 微信开发者工具重新编译并清缓存。
- [ ] 新增自定义动作：目标组数、默认重量、默认次数都可直接输入。
- [ ] 选择动作开始训练：开始设置层目标组数可直接输入。
- [ ] 记录一组：按住 `记录本组` 满 1.5 秒后写入记录。
- [ ] 撤销一组：3 秒内点击撤销，组数、上一组、休息状态恢复。
- [ ] 完成最后一组后撤销：刚生成的历史记录被删除，回到训练中。
- [ ] 休息结束：显示 `休息结束，准备下一组` 并轻震动一次。
- [ ] 训练页停留 120 秒：出现漏记提醒，补记会打开记录 Sheet。
- [ ] 自定义动作管理：列表内自定义动作可进入编辑/删除，历史记录不受删除影响。

## 已知后续项

- Phase 4 可进入“总结卡与展示包装”，重点是训练完成后的截图风格总结卡。
- 主题方案暂不继续沿用“黑琥珀”作为默认方向，撤销提示已从琥珀调整为细边红。
- Tab 图标仍未制作，后续需要 81x81 PNG 素材。
- 当前仍使用本地 storage；云同步和账号系统留到更后阶段。
